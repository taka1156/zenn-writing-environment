---
title: "GitHub Codespacesのスキャフォールディングツールを作ってみた"
emoji: "🪜"
type: "tech"
topics:
  - "docker"
  - "go"
  - "dockercompose"
  - "codespaces"
  - "githubcodespaces"
published: true
published_at: "2026-04-18 10:02"
---



## 概要

Codespaces を使うたびに devcontainer.json を書くのが面倒だったので、自分用の「必要最低限の devcontainer を自動生成する CLI」 を作りました。

## やること

- 以下をスキャフォールディングします。
  - devcontainer.json
  - Dockerfile
  - docker-compose.yaml
- [codespacegen.schema.json](https://github.com/taka1156/codespacegen/blob/master/codespacegen.schema.json)のJSONスキーマに従ってイメージを追加できること

また、以下のような 共通で必要な最低限のパッケージは最初から入れてあります。

- vim
- curl
- tzdata等

## やらないこと（割り切り）

以下は あえて対応していません。

- DB（MySQL / PostgreSQL など）
- ミドルウェア（nginx / Apache）
- mailhog などの開発補助ツール

理由はシンプルで以下のような “スキャフォールディングの本質” に反するためです。
必要なものは 手動で上書きする前提にしています。

- パラメータが多い
- プロジェクトごとに要件が違う
- 使わないことも多い

## Goを選んだ理由

普段はJS/TSメインですが、以下の理由でGoを選びました。

- ランタイムのインストールなしで動く
- バイナリの配布、取り込みが楽
- 簡素な記述ができる

## インストール（curl）

以下のスクリプトを叩くことで、最新リリースを自動でダウンロードし、`/usr/local/bin` に配置できます。

```bash
curl -fsSL https://raw.githubusercontent.com/taka1156/codespacegen/master/scripts/install.sh | bash
```

インストール先を変更する場合:

```bash
curl -fsSL https://raw.githubusercontent.com/taka1156/codespacegen/master/scripts/install.sh | INSTALL_DIR=$HOME/.local/bin bash
```

## 使い方

1. `csg init`を行ってください。
    codespacegen.jsonがカレントディレクトリに生成されるので、それを元に自由に書き換えてください。

    デフォルトは、`codespacegen.json`で認識しますが、`-image-config [ファイル名.json]`とすると任意のファイル名でも動きます。

    :::message
    記載はありませんが、RubyやPHPなど、項目を増やせばいくらでも追加できます。

    それ以外にも、moonbitの例にあるような、インストールが必要なものやgccなどのコンテナに依存しない書き方もできます。
    :::

    :::details codespacegen.json

    ```json
    {
    　"$schema": "codespacegen.schema.json",
    　"common": {
    　  "timezone": "Asia/Tokyo",
        "locale": {
            "lang": "ja_JP.UTF-8",
            "language": "ja_JP:ja",
            "lcAll": "ja_JP.UTF-8"
        },
        "vscodeExtensions": [
            "MS-CEINTL.vscode-language-pack-ja",
            "GitHub.copilot",
            "GitHub.copilot-chat",
            "streetsidesoftware.code-spell-checker",
            "username.errorlens"
        ]
        },
        "langs": [
            {
            　"profileName": "go",
            　"image": "golang:1.24-alpine",
            　"vscodeExtensions": [
                "golang.Go"
            　]
            },
            {
            　"profileName": "node:react",
            　"image": "node:24-alpine",
            　"vscodeExtensions": [
                　"jawandarajbir.react-vscode-extension-pack",
                　"dbaeumer.vscode-eslint",
                　"stylelint.vscode-stylelint",
                　"esbenp.prettier-vscode"
            　　]
            },
            {
            　"profileName": "moonbit",
            　"image": "ubuntu:24.04",
            　"runCommand": "curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash",
            　"vscodeExtensions": [
                　"moonbit.moonbit-lang"
            　]
            },
            {
            　"profileName": "gcc",
            　"image": "ubuntu:24.04",
            　"linuxPackages": [
                "gcc",
                "make",
                "git",
                "binutils",
                "libc6-dev"
            　],
            　"vscodeExtensions": [
                　"ms-vscode.cpptools"
            　]
            },
            {
            　"profileName": "node:zenn",
            　"image": "node:24-alpine",
            　"runCommand": "yarn global add zenn-cli",
            　"vscodeExtensions": [
                "zenn.zenn-vscode-extension"
            　]
            }
        ]
    }
    ```

    :::

2. `csg`で対話形式で擦り合わせながら作成していきます。
※　**対話モード > コマンドライン引数 > json設定** の強さで設定が優先されます。

    ```shell
    $ csg
    プロジェクト名を入力してください（必須）: sample
    言語を入力してください（未入力で alpine 固定）: go
    ワークスペースを入力してください（未入力で /workspace）: (特になければ、enter)
    サービス名を入力してください（未入力で app）: sandbox
    公開ポートを入力してください (例: 3000 または 3000:3000、不要ならEnter): 3000
    タイムゾーンを入力してください（未入力で Asia/Tokyo）: (特になければ、enter)
    ```

3. .devcontainerが現在のディレクトリに出来上がるので追加したいものがあれば適宜追加して調整してください。

その他、`csg update`や`csg version`でそれぞれ更新やバージョン確認ができます。

### cliのオプション

以下、CLIとしてのオプションもつけてみました。

:::details 主なオプション

|     オプション      |             デフォルト              | 説明                                                                                                                             |
| :-----------------: | :---------------------------------: | :------------------------------------------------------------------------------------------------------------------------------- |
|      `-output`      |           `.devcontainer`           | 出力先ディレクトリ                                                                                                               |
|       `-name`       |          *(対話入力必須)*           | プロジェクト名。毎回確認され、`devcontainer.json` の `name` に反映                                                               |
|     `-language`     |      *(対話入力、Enter で空)*       | プログラミング言語。毎回確認される。空の場合は言語固有の設定を使わず `alpine:latest` を採用                                        |
|     `-service`      |    *(対話入力、Enter で `app`)*     | docker compose のサービス名。毎回確認され、`devcontainer.json` と `docker-compose.yaml` 両方に反映                               |
| `-workspace-folder` | *(対話入力、Enter で `/workspace`)* | コンテナ内ワークスペースパス。毎回確認される                                                                                     |
|     `-timezone`     |            `Asia/Tokyo`             | コンテナ内のタイムゾーン。Dockerfile の `ENV TZ` と timezone 設定に反映                                                          |
|    `-base-image`    |         *(言語デフォルト)*          | Dockerベースイメージを直接指定。`-language` のデフォルトより優先                                                                 |
|   `-image-config`   |         `codespacegen.json`         | ベースイメージ定義のローカルパスまたは `https://` URL。`install` のみ指定してイメージを省略した場合は `alpine:latest` を自動採用 |
|       `-port`       |  *(対話入力、Enter で ports なし)*  | ポート指定。たとえば `3000` は `3000:3000` に自動正規化され、`8080:3000` も利用できます。毎回確認されます                        |
|   `-compose-file`   |        `docker-compose.yaml`        | Composeファイル名                                                                                                                |
|      `-force`       |               `false`               | 既存ファイルを上書き                                                                                                             |

:::

## 工夫した点など

工夫した点は以下になります。

- 設計面
  - 日本語化、スペルチェッカーなど汎用的なものを切り出して設定し、各言語設定とマージできるようにしました。
  - aptやcurlなど専用のコンテナがない処理系でも使えるようにlinuxPacakages配列を追加し、重複は自動で修正するようにしました。
  - JSON Schemaを用意して設定を行できるようにしました。


- 運用面
  1. GitHub リリース機能をこのプロジェクトで初めて使いましたが、効率化するためにtagを打つと自動でクロスコンパイルしてリリースされるGitHub Actionを構築しました。
  2. 早期から、shellでざっくりin/outファイルを用意してスナップショットテストを作成し、CLI自体にもヘッドレスモードを追加してテストしやすくしました。

  ※ 1
  - https://github.com/taka1156/codespacegen/blob/master/scripts/tagged.sh
  - https://github.com/taka1156/codespacegen/blob/master/scripts/build.sh
  - https://github.com/taka1156/codespacegen/blob/master/.github/workflows/release.yml

  ※ 2
  - https://github.com/taka1156/codespacegen/tree/master/e2e

## 今後やりたいこと

- ミドルウェア(nginx、Apache)、DB設定など
必要そうでが定項目多すぎるので、優先度は低いです。

- [chat.disableAIFeaturesを有効にしてあえてAI機能をあらかじめ切るなど](https://github.com/taka1156/codespacegen/issues/83)
環境変数を不用意に読まれたくない、あえて外したいなどの用途向けです。

## あとがき

仕様は Dev Container に準拠しているので、
VS Code + Docker Desktop のローカル開発でもそのまま使えるはずです。

**興味があればこちら：**

https://github.com/taka1156/codespacegen
